import { ActionReducerMap } from '@ngrx/store';


export interface AppState {
  // trainees: fromAuth.State;
}

export const appReducer: ActionReducerMap<AppState, any> = {
  // trainees: fromAuth.authReducer,
};
