import { GroupInputDirective } from "./group-input.directive";

describe("GroupInputDirective", () => {
  it("should create an instance", () => {
    const directive = new GroupInputDirective();
    expect(directive).toBeTruthy();
  });
});
