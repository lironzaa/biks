import { CustomErrorMessages } from "../../../shared/components/inputs/error-input/error-messages";

export interface TraineeFormCustomErrorsInterface {
  name: CustomErrorMessages,
  grade: CustomErrorMessages,
  email: CustomErrorMessages,
  dateJoined: CustomErrorMessages,
  subject: CustomErrorMessages,
}
